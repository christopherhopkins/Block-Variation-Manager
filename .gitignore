node_modules
node_modules/
node_modules/*

# build
# build/
# build/*

.claude/settings.local.json

# Generated by `npm run playground:local` — stripped blueprint without the
# GitHub install step (the working tree is mounted instead). Auto-cleaned
# on script exit but listed here in case the script is killed forcibly.
.blueprint.local.json